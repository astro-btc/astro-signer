# 端口号
PORT=33333
# 监听地址（安全默认仅本机可达）
BIND_HOST=127.0.0.1
# 如果你确实要绑定 0.0.0.0（强烈不建议直接暴露公网），需要显式开启
ALLOW_PUBLIC_BIND=0
# 助记词（非常非常敏感，切勿泄露）
MNEMONIC="hello world"
REMOTE_SIGNER_SECRET="change-me-to-a-long-random-string"
#（可选, 若仅支持内网访问建议留空）IP白名单，多个IP请用英文逗号分隔
IPV4_WHITE_LIST=""
#（可选）当使用反向代理（Nginx/Cloudflare）时，设置为 1 以信任 X-Forwarded-For。
TRUST_PROXY=0
